# dependencies + build output
node_modules
dist
.next
out
coverage
*.tsbuildinfo

# env files (all variants, everywhere)
.env
.env.*
!.env.example
!.env.*.example

# os / editor noise
.DS_Store
Thumbs.db
.idea
.vscode
*.swp

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# caches
.turbo
.cache
.parcel-cache
.eslintcache

# uploads (local storage adapter)
uploads/
apps/api/uploads/

# local-only testing + validation
testing/

# secrets / keys / certs (belt-and-braces, should never be in a repo anyway)
*.pem
*.key
*.crt
*.p12
id_rsa
id_ed25519
secrets.json
credentials.json

# local dev databases
*.sqlite
*.sqlite3
*.db

# runtime artifacts
pids
*.pid
*.seed
