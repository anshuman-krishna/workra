import { TabPlaceholder } from '@/components/room/placeholder';

export default function RoomFilesPage() {
  return (
    <TabPlaceholder
      title="files — coming soon"
      blurb="versioned files with client-aware visibility. arriving in the next phase."
    />
  );
}
