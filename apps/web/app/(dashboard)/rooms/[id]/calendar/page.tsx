import { TabPlaceholder } from '@/components/room/placeholder';

export default function RoomCalendarPage() {
  return (
    <TabPlaceholder
      title="calendar — coming soon"
      blurb="monthly view, heatmap, and overlays. arriving in the next phase."
    />
  );
}
