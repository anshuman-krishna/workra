import { TabPlaceholder } from '@/components/room/placeholder';

export default function RoomChatPage() {
  return (
    <TabPlaceholder
      title="chat — coming soon"
      blurb="per-room threaded conversations with task and session references. arriving in the next phase."
    />
  );
}
