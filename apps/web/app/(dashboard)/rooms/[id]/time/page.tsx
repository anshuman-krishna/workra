import { TabPlaceholder } from '@/components/room/placeholder';

export default function RoomTimePage() {
  return (
    <TabPlaceholder
      title="time — coming soon"
      blurb="sessions with intent, summaries, and a clean timeline. arriving in the next phase."
    />
  );
}
