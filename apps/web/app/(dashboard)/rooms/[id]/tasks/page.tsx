import { TabPlaceholder } from '@/components/room/placeholder';

export default function RoomTasksPage() {
  return (
    <TabPlaceholder
      title="tasks — coming soon"
      blurb="lightweight checklists, subtasks, and assignments. arriving in the next phase."
    />
  );
}
